# from django.db.models import signals
# from django.dispatch import receiver
# from shop.models import Category, Book, Author
# from BookShop.utils import auto_slug
#
#
# @receiver(signals.post_save, sender=Category)
# def auto_slug_category(sender, instance, created, **kwargs):
#     if created:
#         instance.slug =
